; Registers markers and sets reasonable timeouts for CI.

[pytest]
minversion = 6.0
addopts = -q
markers =
    hardware: marks tests that require a real mount (slow / manual)
    motion: marks physical motion tests (dangerous — require explicit opt-in)
    slow: marks long running tests
